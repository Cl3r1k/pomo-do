import { TestBed, async } from '@angular/core/testing';

import { PomoState } from '@app/_models/pomo-state';


describe(`Model: PomoState`, () => {
    it(`Should create an instance (async)`, async(() => {
        // Arrange

        // Act

        // Assert
        expect(new PomoState()).toBeTruthy();
    }));
});
